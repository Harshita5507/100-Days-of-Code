//Write a program to swap the first and last digit of a number.
#include <stdio.h>

int main() {
    int num,i;
    printf("Enter a number that is to be reversed: ");
    scanf("%d", num);

    while (num>0){
        
        num;
    }


    
    return 0;
}